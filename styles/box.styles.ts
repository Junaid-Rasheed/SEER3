import { Box, styled } from '@mui/material';

export const BoxStyles = styled(Box)(() => ({
  padding: '4px',
  borderRadius: '5px',
  marginRight: '2px',
  cursor: 'pointer',
  textTransform: 'uppercase',
}));
