import { styled, Typography } from "@mui/material";

export const Title = styled(Typography)(() => ({
  color: "#fff",
  fontFamily: "Helvetica LT Std C",
  textTransform: "uppercase",
  textAlign: "center",
}));
